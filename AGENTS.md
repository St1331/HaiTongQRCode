# HaiTongQRcode 项目协作规则

## 开始任务前

1. 阅读 `README.md`、`docs/开发基线文档.md`、相关 ADR 和受影响模块。
2. 检查 `git status`，保留用户已有改动。
3. 切换到 `main` 并执行 `git pull --ff-only`。
4. 一个任务只处理一个文档中的任务编号，创建独立分支。

## 实施要求

- 数据库结构变化必须新增 TypeORM migration；生产环境禁止 `synchronize: true`。
- 对外接口变化同步更新 Zod 校验、共享类型、API 文档和测试。
- 不提交真实密钥、生产数据、默认密码或含敏感信息的日志。
- 不通过删除测试、降低断言、使用 `any`、吞掉异常或关闭安全机制让检查通过。
- 不修改与当前任务无关的文件，不提前实现后续任务。

## 完成与交付

1. 运行任务要求的最小测试和所有受影响范围测试。
2. 提交信息包含任务编号并说明结果。
3. 每个任务创建一个 Pull Request，填写完成内容、修改文件、测试结果、迁移影响、风险和限制。
4. 未满足任务完成定义时不得标记完成或合并。

